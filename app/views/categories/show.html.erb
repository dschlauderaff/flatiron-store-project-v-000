<h1><%= @category.title %></h1>

<ul>
<% @category.items.each do |item| %>
    <li><%= link_to item.title, item_path(item) %></li>
    <ul>
        <li><%= item.price_to_currency %></li>
    </ul>
    <%= render partial: "items/button_for_add_to_cart", locals: {item: item} %>
<% end %>
</ul>