<h1><%= @category.title %></h1>

<ul>
<% @category.items.each do |item| %>
    <li><%= link_to item.title, item_path(item) %></li>
    <ul>
        <li><%= "$#{item.price.to_f/100}" %></li>
    </ul>
    <%= render "items/button_for_add_to_cart" %>
<% end %>
</ul>